{
  "name": "redrob-ranker-fullstack",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"uvicorn api:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "install-all": "pip install -r requirements.txt && pip install fastapi uvicorn python-multipart && cd frontend && npm install",
    "predev": "python scripts/export_methodology_json.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
