numpy>=1.26
scikit-learn>=1.3
pandas>=2.0

# Optional upgrade path — only needed if you enable the sentence-transformer
# semantic backend via scripts/precompute_embeddings.py. Not required for the
# default TF-IDF pipeline, which has zero network dependency.
# sentence-transformers>=2.6
